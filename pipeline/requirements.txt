# 병의원 세무·마케팅 자동화 파이프라인
crewai>=0.80
anthropic>=0.40                  # 서류 판독 — PDF·이미지를 그대로 실어 보낸다
langchain-anthropic>=0.3         # CrewAI 가 Claude 를 부를 때 쓴다
notion-client>=2.2
python-dotenv>=1.0
httpx>=0.27                      # Notion 첨부파일 내려받기
